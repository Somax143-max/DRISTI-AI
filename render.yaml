services:
  - type: web
    name: drishti-ai-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    ports:
      - port: 8080
    envVars:
      - key: PORT
        value: 8080
